#include <stdio.h>
#include<stdlib.h>

int main (){
	int limite, contador;
	float nota, notaTotal, media;
	
	printf ("digite até qual numero quer somar: ");
	scanf ("%d", &limite);
	
	for (contador=2; contador <=limite; contador=contador+2){
		
		contadorTotal = contadorTotal + contador
		printf ("a soma dos numeros pares de 0 a %d é %d: ", limite, contadorTotal);
	}
	
	
}

